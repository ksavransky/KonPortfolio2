// import { } from '../actions';

const initialState = {
    emptyState: ''
}

const main = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default main;
